import React from 'react';
import {Switch,Router, Redirect} from "react-router-dom";

const AppRouter = () => {
    return (
        <div>
           AppRouter
        </div>
    );
};

export default AppRouter;